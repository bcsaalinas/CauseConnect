<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="CauseConnect - Connecting Causes with People" />
  <title><%= title %> | CauseConnect</title>

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">

  <link rel="stylesheet" href="/css/main.css">
  <% if (page === 'contact') { %>
    <link rel="stylesheet" href="/css/contact.css">
  <% } else if (page === 'directory') { %>
    <link rel="stylesheet" href="/css/directory.css">
  <% } else if (page === 'sdgs') { %>
    <link rel="stylesheet" href="/css/sdg-section.css">
  <% } %>

  <!-- Google Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&family=Montserrat:wght@400;500;600;700&display=swap" rel="stylesheet">
  <script>
    window.APP_ANIM = window.APP_ANIM || { enabled: true, debug: false };
  </script>
  <script>document.documentElement.classList.add('has-js');</script>
</head>

<body>
  <a class="skip-link" href="#main-content">Skip to content</a>
  <!-- Navbar -->
  <header>
    <div class="cc-header-shell">
      <div class="blur-blob hero-blob d-none d-md-block"></div>
      <nav class="navbar navbar-expand-md navbar-dark cc-header cc-header-minimal sticky-top cc-header-glass">
        <div class="container-xl">
          <a href="/" class="navbar-brand d-inline-block">
            <img src="/images/logo.png" alt="Cause Connect" class="cc-brand-logo" />
          </a>
          <button
            class="navbar-toggler"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent"
            aria-expanded="false"
            aria-label="Toggle navigation"
          >
            <span class="navbar-toggler-icon"></span>
          </button>

          <div class="collapse navbar-collapse flex-md-row flex-column align-items-md-center align-items-stretch gap-2 gap-md-3" id="navbarSupportedContent">
            <ul class="navbar-nav ms-md-auto align-items-md-center align-items-start gap-md-2">
              <li class="nav-item">
                <a class="nav-link <%= page === 'home' ? 'active' : '' %>" href="/" <%= page === 'home' ? 'aria-current="page"' : '' %>>Home</a>
              </li>
              <li class="nav-item">
                <a class="nav-link <%= page === 'directory' ? 'active' : '' %>" href="/directory" <%= page === 'directory' ? 'aria-current="page"' : '' %>>Directory</a>
              </li>
              <li class="nav-item">
                <a class="nav-link <%= page === 'sdgs' ? 'active' : '' %>" href="/sdgs" <%= page === 'sdgs' ? 'aria-current="page"' : '' %>>SDGs</a>
              </li>
              <li class="nav-item">
                <a class="nav-link <%= page === 'contact' ? 'active' : '' %>" href="/contact" <%= page === 'contact' ? 'aria-current="page"' : '' %>>Contact</a>
              </li>
            </ul>

            <form role="search" class="d-flex ms-md-4 ms-0 cc-search align-items-center gap-2">
              <span class="visually-hidden" id="site-search-label">Search directory</span>
              <input class="flex-grow-1" type="search" placeholder="Search" aria-labelledby="site-search-label" />
            </form>

            <button type="button" class="btn btn-pill btn-pill-outline ms-md-3 my-md-auto my-2" id="login">
              Login
            </button>
            <button type="button" class="btn btn-pill btn-pill-primary ms-md-2 my-md-auto my-2" id="sign-up">
              Sign-up
            </button>
          </div>
        </div>
      </nav>
    </div>
  </header>
  <main id="main-content" class="<%= page === 'home' ? 'page-main page-main--flush' : 'page-main' %>">
